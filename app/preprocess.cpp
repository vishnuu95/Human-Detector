#include <bits/stdc++.h>
#include "opencv2/opencv.hpp"
#include "preprocess.hpp"

using namespace vision;

void Preprocessor::preprocess(pair<int, int> size, int blurKernel, cv::Mat& inImg, cv::Mat& outImg) {
	return;	
}