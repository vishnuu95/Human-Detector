#include <gtest/gtest.h>
#include <bits/stdc++.h>
#include "opencv2/opencv.hpp"
#include <opencv2/tracking/tracker.hpp>
#include <Eigen/Dense>
#include "robot.hpp"

TEST(robotTest, initialisation) {
  EXPECT_EQ(1, 1);
}


TEST(robotTest, dataProcessing) {
  EXPECT_EQ(1, 1);
}

TEST(ioHandling, initialisation) {
  EXPECT_EQ(1, 1);
}


TEST(ioHandling, correctParsing1) {
  EXPECT_EQ(1, 1);
}

TEST(detectionAndTracking, imageTest) {
  EXPECT_EQ(1, 1);
}


TEST(detectionAndTracking, videoTest) {
  EXPECT_EQ(1, 1);
}


TEST(preprocessing, imageTest) {
  EXPECT_EQ(1, 1);
}


TEST(detectionAndTracking, imageTest) {
  EXPECT_EQ(1, 1);
}


TEST(transformation, initialisation) {
  EXPECT_EQ(1, 1);
}

TEST(transformation, transformCords) {
  EXPECT_EQ(1, 1);
}

TEST(depthEstimation, initialisation) {
  EXPECT_EQ(1, 1);
}


TEST(depthEstimation, estimate) {
  EXPECT_EQ(1, 1);
}